---
import { Icon } from "astro-icon/components";
import Button from "../ui/button.astro";

const currentYear = new Date().getFullYear();

const navLinks = [
  { href: "/", label: "Inicio" },
  { href: "/gallery", label: "Portafolio" },
  { href: "/services", label: "Servicios" },
  { href: "/about", label: "Sobre Mí" },
  { href: "/contact", label: "Contacto" },
];

const socialLinks = [
  { href: "#", icon: "lucide:instagram", label: "Instagram" },
  { href: "#", icon: "lucide:linkedin", label: "LinkedIn" },
  { href: "#", icon: "lucide:twitter", label: "Twitter/X" },
  { href: "#", icon: "lucide:camera", label: "Behance" },
];
---

<footer
  class="bg-background border-t border-white/5 pt-24 pb-12 overflow-hidden relative"
>
  <!-- Decorative Background Glow -->
  <div
    class="absolute bottom-0 right-0 w-1/3 h-1/2 bg-primary/5 blur-[120px] rounded-full -z-10"
  >
  </div>

  <div class="container mx-auto px-6">
    <!-- Top Contact CTA -->
    <div
      class="flex flex-col md:flex-row items-center justify-between gap-8 mb-24 pb-24 border-b border-white/5"
    >
      <div class="max-w-xl text-center md:text-left space-y-4">
        <h2
          class="text-3xl md:text-5xl font-serif font-bold text-foreground leading-tight"
        >
          ¿Tienes una historia <br /> que <span class="text-primary italic"
            >necesitas</span
          > contar?
        </h2>
        <p class="text-muted-foreground text-lg">
          Hablemos sobre tu próximo proyecto y creemos algo extraordinario
          juntos.
        </p>
      </div>
      <div class="flex flex-col sm:flex-row gap-4 shrink-0">
        <Button href="/contact" size="lg" rounded="full">
          Empezar Proyecto
        </Button>
        <Button
          href="mailto:hola@lenscraft.com"
          variant="outline"
          size="lg"
          rounded="full"
        >
          Enviar Email
        </Button>
      </div>
    </div>

    <!-- Main Footer Content -->
    <div
      class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-12 gap-12 lg:gap-16 mb-24"
    >
      <!-- Brand & Social -->
      <div class="lg:col-span-4 space-y-8">
        <div class="flex items-center gap-2">
          <div
            class="w-10 h-10 flex items-center justify-center bg-primary rounded-lg shadow-lg shadow-primary/20"
          >
            <Icon
              name="lucide:camera"
              class="w-5 h-5 text-primary-foreground"
            />
          </div>
          <span
            class="text-2xl font-serif font-bold tracking-tighter text-foreground"
          >
            LensCraft
          </span>
        </div>
        <p class="text-muted-foreground leading-relaxed">
          Fotografía profesional de autor enfocada en la narrativa emocional y
          la excelencia técnica. Transformando instantes en legados visuales
          eternos.
        </p>
        <div class="flex gap-4">
          {
            socialLinks.map((social) => (
              <a
                href={social.href}
                aria-label={social.label}
                class="w-10 h-10 flex items-center justify-center rounded-full bg-secondary/50 border border-white/5 text-muted-foreground hover:text-primary hover:border-primary/20 hover:scale-110 transition-all duration-300"
              >
                <Icon name={social.icon} class="w-5 h-5" />
              </a>
            ))
          }
        </div>
      </div>

      <!-- Quick Links -->
      <div class="lg:col-span-4">
        <h4
          class="text-foreground font-bold uppercase tracking-widest text-xs mb-8"
        >
          Navegación
        </h4>
        <ul class="space-y-4">
          {
            navLinks.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-muted-foreground hover:text-primary transition-colors duration-300"
                >
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <!-- Contact Info -->
      <div class="lg:col-span-4 space-y-8">
        <h4
          class="text-foreground font-bold uppercase tracking-widest text-xs mb-8"
        >
          Contacto
        </h4>
        <div class="space-y-6">
          <div class="flex items-start gap-4">
            <div
              class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center shrink-0"
            >
              <Icon name="lucide:mail" class="w-5 h-5 text-primary" />
            </div>
            <div>
              <p class="text-xs text-muted-foreground uppercase font-bold mb-1">
                Escríbenos
              </p>
              <p class="text-foreground font-medium">hola@lenscraft.com</p>
            </div>
          </div>
          <div class="flex items-start gap-4">
            <div
              class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center shrink-0"
            >
              <Icon name="lucide:phone" class="w-5 h-5 text-primary" />
            </div>
            <div>
              <p class="text-xs text-muted-foreground uppercase font-bold mb-1">
                Llámanos
              </p>
              <p class="text-foreground font-medium">+34 600 000 000</p>
            </div>
          </div>
          <div class="flex items-start gap-4">
            <div
              class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center shrink-0"
            >
              <Icon name="lucide:map-pin" class="w-5 h-5 text-primary" />
            </div>
            <div>
              <p class="text-xs text-muted-foreground uppercase font-bold mb-1">
                Estudio
              </p>
              <p class="text-foreground font-medium">
                Calle de las Artes, 12, 28001 Madrid, España
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Bottom Copyright -->
    <div
      class="flex flex-col md:flex-row items-center justify-between gap-6 pt-12 border-t border-white/5"
    >
      <p class="text-sm text-muted-foreground">
        &copy; {currentYear} LensCraft. Todos los derechos reservados.
      </p>
      <div class="flex gap-8">
        <a
          href="/legal"
          class="text-xs text-muted-foreground hover:text-primary transition-colors"
          >Aviso Legal</a
        >
        <a
          href="/privacy"
          class="text-xs text-muted-foreground hover:text-primary transition-colors"
          >Privacidad</a
        >
        <a
          href="/cookies"
          class="text-xs text-muted-foreground hover:text-primary transition-colors"
          >Cookies</a
        >
      </div>
    </div>
  </div>
</footer>
